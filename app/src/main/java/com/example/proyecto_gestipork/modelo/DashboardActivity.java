package com.example.proyecto_gestipork.modelo;

import android.os.Bundle;
import android.view.Menu;
import android.view.MenuItem;
import android.widget.Spinner;

import androidx.annotation.NonNull;
import androidx.appcompat.widget.Toolbar;


import androidx.appcompat.app.AppCompatActivity;

import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;

public class DashboardActivity extends AppCompatActivity {
/*
    private Spinner spinnerExplotaciones;
    private RecyclerView recyclerResumen;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_dashboard);

        Toolbar toolbar = findViewById(R.id.toolbar_dashboard);
        setSupportActionBar(toolbar);

        spinnerExplotaciones = findViewById(R.id.spinner_explotaciones);
        recyclerResumen = findViewById(R.id.recycler_resumen);

        // TODO: Cargar explotaciones en spinner

        recyclerResumen.setLayoutManager(new LinearLayoutManager(this));
        // TODO: Configurar el adaptador personalizado para mostrar las dos CardView
    }

    @Override
    public boolean onCreateOptionsMenu(Menu menu) {
        getMenuInflater().inflate(R.menu.menu_dashboard, menu);
        return true;
    }

    @Override
    public boolean onOptionsItemSelected(@NonNull MenuItem item) {
        switch (item.getItemId()) {
            case R.id.menu_add_explotacion:
                // TODO: ir a pantalla para añadir explotación
                return true;
            case R.id.menu_logout:
                // TODO: cerrar sesión y volver a login
                return true;
            default:
                return super.onOptionsItemSelected(item);
        }
    }*/
}
