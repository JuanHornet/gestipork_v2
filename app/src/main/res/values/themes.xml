<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Proyecto_Gestipork" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/principal</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <style name="PopupMenuOverlayWhiteIcon" parent="ThemeOverlay.Material3.Light">
        <!-- Evita heredar iconos oscuros -->
        <!-- Esto puede afectar el color del icono de overflow -->
        <item name="android:textColor">@android:color/black</item> <!-- texto del menú -->
        <item name="android:tint">@android:color/white</item>     <!-- icono de overflow -->
    </style>
    <style name="TabTextStyle" parent="TextAppearance.Material3.LabelLarge">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#000000</item> <!-- o usa @color/black -->
    </style>
    <style name="CustomDatePickerDialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:datePickerStyle">@style/CustomDatePicker</item>
    </style>

    <style name="CustomDatePicker" parent="android:Widget.Material.DatePicker">
        <item name="android:calendarViewShown">true</item>
        <item name="android:spinnersShown">false</item>
    </style>



    <style name="Theme.Proyecto_Gestipork" parent="Base.Theme.Proyecto_Gestipork" />
</resources>