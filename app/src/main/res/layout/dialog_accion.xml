<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Tipo de acción (Spinner) -->
    <Spinner
        android:id="@+id/spinner_tipo_accion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp" />

    <!-- Fecha -->
    <EditText
        android:id="@+id/edit_fecha_accion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Fecha"
        android:focusable="false"
        android:inputType="none"
        android:layout_marginBottom="12dp" />

    <!-- Nº Animales -->
    <EditText
        android:id="@+id/edit_cantidad"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nº animales"
        android:inputType="number"
        android:layout_marginBottom="12dp" />

    <!-- Observaciones -->
    <EditText
        android:id="@+id/edit_observaciones"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:hint="Observaciones"
        android:gravity="top"
        android:inputType="textMultiLine"
        android:layout_marginBottom="16dp" />

    <!-- Botones -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <Button
            android:id="@+id/btn_cancelar_accion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancelar" />

        <Button
            android:id="@+id/btn_guardar_accion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Guardar"
            android:layout_marginStart="16dp" />
    </LinearLayout>
</LinearLayout>
